# Flast-API-December-2025
Flask API

# Docker build command 
docker build -t flask_loan_app .
# Docker build command (1st command used for Docker hub and second was used for AWS Container hub)
docker build --platform=linux/amd64 -t flask_loan_app .
docker build --platform=linux/amd64 -t dheeraj_flask_loan_app .
